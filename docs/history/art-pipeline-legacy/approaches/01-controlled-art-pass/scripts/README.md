# Scripts

Reusable deterministic helpers for guide rasterization, mask restoration, extraction and QA belong here. Production scripts that are used by the build may live in top-level `scripts/` and should be linked from this folder.