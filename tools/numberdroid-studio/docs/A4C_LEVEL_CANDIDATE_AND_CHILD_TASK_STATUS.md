# Numberdroid Studio — A4c Level Candidate and derived child status

Status: **SOURCE-INTEGRATED / AUTOMATED, BROWSER AND WINDOWS GREEN — NOT USER ACCEPTED**

Date: 2026-09-01

The complete currently authorized A4c source sequence is integrated on `main`
at `122a2533a12abe5e51e6458fa80f2047825240b4`, tree
`c14f39c687214434ab7b8850dfc4411bf2fe1a60`. It contains the authority-neutral
Domain foundation, one private immutable Level Candidate application closure,
and the separately bounded trusted-service derivation of one strictly
attenuated child task. It does not accept A4c, authorize A5, or grant any
materialization, repository-publication or release authority.

## Integration ledger

| Slice | Exact PR head | Merge | Exact-head CI | Post-merge CI |
| --- | --- | --- | --- | --- |
| Authority-neutral Domain DTO foundation | PR #186 `c70019e5d510dcc06d2593b0258bd1970a0e2a6d` | `93187bfd039e00147ce930a937ff0801c09c9784` | Build #2279 / `33365246228` | Build #2280 / `33368143760` |
| Immutable Level Candidate application | PR #198 `6ea743ff5570677c24a58fae2c5cad210af19d3d` | `053f9c407f5ecd7c93c1775249aa0630c88b6460` | Build #2344 / `33553937139` | Build #2345 / `33554781224`, failed-only rerun green |
| Restricted derived child task | PR #199 `6f7c6bfee89fec2e22200fe66002b3d3c73fb968` | `122a2533a12abe5e51e6458fa80f2047825240b4` | Build #2347 / `33562709548` | Build #2348 / `33563416584`, failed-only rerun green |

The final PR #199 head has tree
`c14f39c687214434ab7b8850dfc4411bf2fe1a60`, identical to its merge tree. The
implementation head was rechecked unchanged before merge. Build #2348 attempt
1 reached the exact ten-minute Windows job limit after its test output had
remained green; only failed jobs were rerun on the same merge commit and attempt
2 completed Windows, Checkpoint 1A and the final gate successfully. That bounded
rerun is CI evidence, not acceptance.

## Immutable Candidate closure

The private `level.candidate.create` application path uses the real A4b
reference LevelSpec, A4a projection, A3a validation, Numberdroid adapter/compiler
and configured validate-only EngineBridge. One exact task/grant/actor/project,
branch base and current head, capability profile, compiler pins, immutable
source, complete output bytes/hashes, deterministic preview, semantic/output
diff, receipt and submission form one fail-closed closure.

Successful submission is atomic, durable, idempotent and lost-response-safe. It
leaves the task and effective state at `IN_REVIEW`, creates an `OPEN` review with
all items `PENDING`, appends `REVIEW_SUBMITTED`, preserves the exact branch head,
and presents **Waiting for your review**. `main` remains unchanged. The Candidate
contains no decision, merge, destination, materialization, filesystem/repository
write, publication or release operation.

Candidate persistence is schema v14 in
`packages/persistence/src/sqlite/migrations/0014_level_candidate_submissions.sql`.
Explicit replay after restart revalidates its semantic closure; the admin verify
path and deep-integrity tests inspect the persisted closure. Submission/replay
fails closed for stale heads, authority or binding drift,
compiler/profile/bridge mismatch, tamper, hostile data and partial state.

## Restricted derived child closure

The separate private trusted-service operation derives a child only from the
exact current active human-rooted parent task and grant. The service, never the
request, fixes the same actor/project, an exact non-main branch origin, the
`level.candidate.create` scope, a no-later expiry and policy-bounded reservation
of all four budget dimensions. Scope and object authority cannot widen,
automatic acceptance is disabled, depth is limited to one, and complete
ancestor checks gate every child operation.

Creation is atomic, audited, idempotent and restart-safe. One human root may
produce at most one such child. Parent pause/review blocks mutation; cancellation,
rejection, merge, expiry or grant revocation makes the child non-executable. The
historical derivation origin remains an immutable fact after later parent-head
movement, while execution correctly fails as stale.

Child persistence is schema v15 in
`packages/persistence/src/sqlite/migrations/0015_derived_candidate_child_tasks.sql`
with checksum
`12657571e580597ed6ab03459c3e1241c0c94642e32d0e86bef087e232998154`.
Relation, reservation, grant and lineage integrity are checked deeply; every
declared fault point rolls back without a partial task, branch, grant or budget
reservation.

## Read-only human projection

The existing task overview/detail surface can present the immutable Candidate
and child origin, lineage, reservation and blocked-ancestor state. It supplies
no create/derive controls and no owner decision or merge action. A selected
trusted-service child does not request the unrelated processing-result-adoption
projection. The detail therefore stays readable even when that separate service
is unavailable and does not pretend that adoption authority belongs to the
child.

Build #2347 captured the actual child surface at 1440 and 1060 pixels in the
`numberdroid-studio-checkpoint-4-evidence` artifact, including the blocked
ancestor state and absence of adoption controls. Browser evidence proves the
rendered implementation only; it cannot approve the authority model or product
experience.

## Verification closure

The final combined source passed locally:

- the complete Studio suite: 747 total, 743 passed, four expected platform
  skips, zero failures;
- the focused Candidate/child closure: 30 of 30 passed;
- Studio JavaScript syntax checking: 239 files;
- the real A4c vertical composer suite: six of six passed;
- protected Checkpoint 1A evidence: `VERIFIED`; and
- the root suite before the final Studio-only browser repair: 46 files and 230
  tests passed; exact-head Build #2347 reran and passed that root gate.

Build #2346 / run `33561494607` was a diagnostic run on the superseded PR head.
Its browser gate exposed the unrelated adoption request described above. The
fix was reviewed independently for runtime behavior, interaction/UX,
security/authority, persistence/idempotency, actual test/CI scope and combined
cross-domain correctness before fresh exact-head Build #2347 passed Linux,
browser, Windows, root and final gates.

## Unchanged boundaries and remaining gate

EngineBridge remains validate-only. Neither A4c slice extends generic Studio
Preview, runtime preview, MCP/resource discovery or any accepted 19/4, 30/5 or
selected 31/6 compatibility catalog. There is no Remote backup, Pairing,
HostBinding, public/Funnel, browser-agent, root/grant-management, auto-accept,
owner review-decision/acceptance operation, merge, `main` append, activation, deletion, cleanup,
materialization, repository-publication or release authority.

Klaus still owns the product/contract decision recorded as VT-014. Source
integration, compiler output, screenshots, independent review and CI do not
close it. Schema v14 and v15 are forward-only for an upgraded real workspace;
recovery uses a verified pre-upgrade copy or a forward repair, never an
unsupported downgrade. The currently authorized A4c source sequence is
complete; A5, A6 or any wider operational work requires a separate explicit
scope decision.
